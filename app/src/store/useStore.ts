// Re-export the Supabase-integrated store
// The store now supports both authenticated (Supabase) and demo (mock data) modes
export { useStore } from './useSupabaseStore';
export type { Screen } from './useSupabaseStore';
