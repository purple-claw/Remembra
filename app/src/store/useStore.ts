// Re-export the Firebase-integrated store
// The store supports authenticated mode and local persisted UI state.
export { useStore } from './useSupabaseStore';
export type { Screen } from './useSupabaseStore';
